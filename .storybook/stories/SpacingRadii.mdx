import { Meta } from "@storybook/blocks";

<Meta title="Tokens/Spacing & Radii" />

# Spacing

<div style={{ display: "grid", gap: "var(--space-2)" }}>
  {["0","1","2","3","4","6","8"].map((k) => (
    <div key={k} style={{ display: "flex", alignItems: "center", gap: "var(--space-3)" }}>
      <code style={{ width: 80 }}>space.{k}</code>
      <div style={{ height: 16, width: `var(--space-${k})`, background: "var(--color-brand-primary-500)" }} />
    </div>
  ))}
</div>

# Radii

<div style={{ display: "flex", gap: "var(--space-4)", marginTop: "var(--space-4)" }}>
  {["sm","md","lg","full"].map((k) => (
    <div
      key={k}
      style={{
        width: 72,
        height: 72,
        background: "var(--color-brand-primary-500)",
        borderRadius: `var(--radius-${k})`,
        display: "grid",
        placeItems: "center",
        color: "var(--color-neutral-0)",
        fontFamily: "var(--font-family-sans)",
      }}
    >
      {k}
    </div>
  ))}
</div>
